apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api
  namespace: heart-disease
spec:
  type: LoadBalancer
  selector:
    app: heart-disease-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
